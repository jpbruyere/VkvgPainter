CheckBox {
	Background="Transparent";
	Checked="{Background=RoyalBlue}";
	Unchecked="{Background=Transparent}";
}
RadioButton {
	Width="Fit";
	Height="Fit";
	Background="DimGrey";
	Checked="{Background=RoyalBlue}";
	Unchecked="{Background=DimGrey}";
}
EnumSelector {
	Height="Fit";
	Foreground="Jet";

}
DrawModeStyle {
	Template="#ui.iconCheckBox.template";
	//Initialized="GetDrawModeIcon";
	//Initialized="{Background=Blue}";
}